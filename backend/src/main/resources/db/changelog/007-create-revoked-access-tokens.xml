<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Lista de revocación (blacklist) de access tokens JWT. Al cerrar sesión
        (manual o por inactividad) se registra el identificador único del token
        (claim "jti") para que deje de ser aceptado aunque aún no haya expirado.
        También funciona como bitácora de cierres de sesión (usuario, motivo, IP).
    -->
    <changeSet id="007-1-create-revoked-access-tokens-table" author="umg">
        <createTable tableName="revoked_access_tokens">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_revoked_access_tokens"/>
            </column>
            <column name="jti" type="VARCHAR(64)">
                <constraints nullable="false" unique="true" uniqueConstraintName="uq_revoked_access_tokens_jti"/>
            </column>
            <column name="username" type="VARCHAR(50)"/>
            <column name="reason" type="VARCHAR(30)">
                <constraints nullable="false"/>
            </column>
            <column name="client_ip" type="VARCHAR(64)"/>
            <column name="expires_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="revoked_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP"/>
        </createTable>
        <createIndex tableName="revoked_access_tokens" indexName="idx_revoked_access_tokens_expires_at">
            <column name="expires_at"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
